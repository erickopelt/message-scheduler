apiVersion: v1
kind: Service
metadata:
  labels:
    app: scheduler
  name: scheduler-service
spec:
  ports:
    - port: 8080
      protocol: TCP
      nodePort: 30003
      name: http
  selector:
    app: app
  type: NodePort
